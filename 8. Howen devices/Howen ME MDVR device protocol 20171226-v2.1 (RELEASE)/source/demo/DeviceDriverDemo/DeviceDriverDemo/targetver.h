#pragma once

// 包括 SDKDDKVer.h 将定义可用的最高版本的 Windows 平台。

// 如果要为以前的 Windows 平台生成应用程序，请包括 WinSDKVer.h，并将
// 将 _WIN32_WINNT 宏设置为要支持的平台，然后再包括 SDKDDKVer.h。

// include SDKDDKVer.h will define workable highest windows OS 

// If you want to build an application for the previous Windows platform，please include WinSDKVer.h，
// and Set the _WIN32_WINNT macro to the platform to support, and then include SDKDDKVer.h

#include <SDKDDKVer.h>
